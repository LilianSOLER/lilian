import { Resetter } from '../utils/resetter.js';
import { QueueItem } from './queue-item.js';
export declare class Queue {
    private readonly _resetter;
    private readonly _items;
    private _queueIndex;
    private _queueDetailIndex;
    constructor(resetter: Resetter);
    add(item: QueueItem): void;
    clear(): void;
    resetIndices(): void;
    get item(): QueueItem;
    get detailIndex(): number;
    increment(maxDetailIndex?: number): boolean;
    private nextQueueItem;
}
