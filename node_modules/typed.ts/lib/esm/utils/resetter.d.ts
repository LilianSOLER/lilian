export declare class Resetter {
    private _reset;
    private _resolveReset;
    private _resetPromise;
    private _singleReset;
    private _resolveSingleReset;
    private _resetSinglePromise;
    reset(): Promise<void>;
    private resetResetter;
    get isReset(): boolean;
    get resetPromise(): Promise<void>;
    singleReset(): void;
    resetSingleResetter(): void;
    get isSingleReset(): boolean;
    get resetSinglePromise(): Promise<void>;
}
