import { keyboards } from '../data/keyboards.js';
export const ensureLocale = (locale) => {
    if (!keyboards[locale]) {
        throw new Error(`Locale ${locale} is not known`);
    }
};
//# sourceMappingURL=ensure-locale.js.map